/**
 * Preview Mode   " full-canvas simulation preview (Plan 06   3.2).
 * Consumed by shell studio registry and runtime-standalone.
 * Target folder per Plan 06   3.2 (`studios/preview/**`).
 */
import './styles/PreviewStudio.css';

export { PreviewMode } from './PreviewMode';
